const header=document.querySelector('.site-header');const menu=document.querySelector('#menu');const menuButton=document.querySelector('.menu-button');
addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>30));menuButton.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuButton.setAttribute('aria-expanded',open)});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));
const form=document.querySelector('#diagnostic');let step=1;const progress=form.querySelector('.progress span');const number=document.querySelector('#stepNumber');const next=form.querySelector('.next');const back=form.querySelector('.back');
function showStep(){form.querySelectorAll('fieldset').forEach(f=>f.hidden=Number(f.dataset.step)!==step);number.textContent=step;progress.style.width=`${step/3*100}%`;back.hidden=step===1;next.textContent=step===3?'Lag oppsummering':'Neste'}
next.addEventListener('click',()=>{if(step===1&&!form.querySelector('[name=situation]:checked')){alert('Velg alternativet som passer best.');return}if(step<3){step++;showStep();return}const data=new FormData(form);const situation=data.get('situation');const outcome=data.get('outcome')||'et tydelig, konkret resultat';const blocker=data.get('blocker')||'dagens viktigste hindring';form.querySelectorAll('fieldset,.tool-actions,.step-label').forEach(el=>el.hidden=true);progress.style.width='100%';const result=form.querySelector('.tool-result');result.hidden=false;document.querySelector('#summary').textContent=`Dere beskriver situasjonen slik: ${situation}. Målet er ${outcome.toLowerCase()}, mens ${blocker.toLowerCase()} står i veien. Et godt første steg er å avgrense én beslutning eller prosess som kan flyttes merkbart de neste 30 dagene.`});
back.addEventListener('click',()=>{if(step>1){step--;showStep()}});document.querySelector('#year').textContent=new Date().getFullYear();
